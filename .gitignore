# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/

# Environments
.venv/
.env
.env.*
pip-wheel-metadata/

# AI tooling (local-only)
.claude/

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Outputs (generated artifacts must not be versioned)
outputs/*
!outputs/.gitkeep

# Generated figures, HTML reports, and serialized models (regenerable from the
# pipeline with random_state=42; numeric results stay tracked as CSV/JSON/parquet)
*.png
*.jpg
*.jpeg
*.svg
*.pdf
*.html
*.joblib

# Local data (keep raw inputs out of Git; track via docs/data_manifest.md instead)
data_raw/
data_intermediate/

# Secrets / credentials
*credentials*
*secret*
*token*

# Raw inputs (local-only; provenance recorded in docs/)
admin_demographics/boundaries_raw/
admin_demographics/demographics_raw/
admin_demographics/boundaries_export/

# Run logs (regenerable transcripts)
run_aza.log

# Oversized generated EDA table (151 MB, exceeds GitHub's 100 MB limit;
# regenerable via eda/run_eda.py)
eda/outputs*/tables/outlier_observations.csv
